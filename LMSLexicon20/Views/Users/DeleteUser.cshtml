@model LMSLexicon20.Models.ViewModels.UserDeleteViewModel

@{
    ViewData["Title"] = "Ta bort användare";
}

<h2 class="bg-dark shadow-lg border rounded-lg font-weight-bold text-light p-4">TA BORT ANVÄNDARE</h2>
<h4>Är du säker på att du vill ta bort användaren?</h4>
@*@if (Model.Teacher != null)
    {
        <a asp-controller="Users" asp-action="Details" asp-route-id="@Model.Teacher.Id">@($"{Model.Teacher.FirstName} {Model.Teacher.LastName}")</a>
    }*@
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FullName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FullName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RegDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RegDate)
        </dd>
    </dl>

    <form asp-action="DeleteUserConfirmed">
        <input type="hidden" asp-for="Id" />
        @if (User.Identity.Name == Model.Email)
        {
            <div class="text-danger">Du kan inte ta bort kontot du använder!</div>
            <button class="btn btn-danger" disabled >Ta bort</button>
        }
        else{<input class="btn btn-danger" type="submit" value="Ta bort" />}
        @*<a class="btn btn-secondary" asp-controller="Courses" asp-action="Details" asp-route-id="@Model.Course.Id">Avbryt</a>*@
        <a class="btn btn-secondary" asp-action="Details" asp-route-id="@Model.Id">Avbryt</a>
    </form>
</div>
